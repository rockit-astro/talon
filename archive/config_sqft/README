dome.cfg  	DOMEHAVE = 1          		! 1 to enable rotating dome control, 0 to disable.
		SHUTTERHAVE = 1     		! 1 to control shutter/roof, 0 if none.

filter.cfg:4:IHAVE              	0      !1         ! 1 for filter wheel control, 0 to disable.

focus.cfg  +8 	OHAVE               	0      !1       ! 1 if even have a focus motor, 0 if not.
focus.cfg  +19	OHAVEENC   		1               ! true if we're using encoder


gpsd.cfg +3 	HAVEGPS      		1               ! 1 if even have a gps, else 0


telescoped.cfg +15 HHAVE         0      !1         ! 1 if H axis is to be active, 0 if not
telescoped.cfg +25 DHAVE         0      !1         ! 1 if D axis is to be active, 0 if not
telescoped.cfg +36 RHAVE         0                 ! 1 if even have an image rotator, 0 if not
telescoped.cfg +55 LADDR         -1     !0         ! csimc addr for light control, else -1
