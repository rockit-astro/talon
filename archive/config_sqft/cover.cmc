define cover($open)
{
	if($open) {
		//signal the node
		olevel= (olevel&0x3F)|0x80;
	} else {
		olevel= (olevel&0x3F)|0x40;
	}

	printf("0:cover in place\n");
}



define coverOpen() {
	cover(1);
}

define coverClose() {
	cover(0);
}
