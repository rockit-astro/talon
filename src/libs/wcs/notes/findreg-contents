Contents of libwcs/findreg.c  (file dated 2001 Apr 20 06:12:14 on onem)
----------------------------

o Various #include header files
o Various #define quantities
o Triangle structure typedef
o Various static variables
o Declarations of all static functions defined below except chisqr, d_cmp
o Function definitions:
   findRegistration (main purpose of this file, matching and astrometry)
   init_fim         (static)
   gentri           (static)
   chisqr           (static, name passed as arg. to lstsqr in call_lstsqr)
   call_lstsqr      (static)
   d_cmp            (static, name passed as argument to qsort in dmedian)
   dmedian          (static)
   setFITSWCS       (static)

Function references in findRegistration
   init_fim         this file
   setFITSWCS       this file
   RADec2xy         libwcs/xyradec.c
   gentri           this file
   call_lstsqr      this file
   dmedian          this file
   resetFImage      libfits/fits.c

Function references in init_fim
   initFImage       libfits/fits.c
   setLogicalFITS   libfits/fits.c
   setIntFITS       libfits/fits.c

Function references in gentri
   sqrt             <math.h>

Function references in chisqr
   setFITSWCS       this file
   RADec2xy         libwcs/xyradec.c
   atan2            <math.h>
   sqrt             <math.h>

Function references in call_lstsqr
   lstsqr           libmisc/lstsqr.c

Function references in setFITSWCS
   setStringFITS    libfits/fits.c
   setRealFITS      libfits/fits.c
